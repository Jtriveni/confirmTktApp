class IosInfo {
    static deviceName() {
        return 'iPhone 14'; // pass the device name
    }

    static platFormVersion() {
        return '16.4'; // pass the platform version
    }

    static appName() {
        return 'com.apple.Preferences'; // pass the bundleid or ipa name
    }
}

module.exports = IosInfo;
